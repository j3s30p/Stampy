{
  "compilerOptions": {
    "strict": true
  },
  "fmt": {
    "lineWidth": 100,
    "semiColons": true,
    "singleQuote": true
  },
  "tasks": {
    "quality": "deno fmt --check && deno lint && deno check sync-stamp-spots/index.ts && deno test"
  }
}
